.move-board {
  position: relative;
  width: 100%;
  height: 100%;
  overflow: hidden;
  user-select: none;

  .move-board-item {
    position: absolute;
    cursor: grab;
    transition: transform 0.1s ease-out;
  }
}