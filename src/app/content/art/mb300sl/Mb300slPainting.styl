.mb-300sl-painting {
  .text-container {
    a {
      position: sticky;
      z-index: 2;
    }
    .image-container {
      text-align: center;
      margin: 20px auto;
      width: 50%;
      transform: rotate(3deg);
      img {
        width: 100%;
        height: 100%;
        object-fit: cover;
      }
    }
  }
}