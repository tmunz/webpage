.frame.photo-frame.active {
  .background-image {
    width: 100%;
    height: 100%;
    left: 0;
    top: 0;
    right: 0;
    bottom: 0;
    transform: scale(4) translate(-5%, 20%);
  }
}

.photo {
  height: 100%;
}