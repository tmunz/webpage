.drag-board {
  position: relative;
  width: 100%;
  height: 100%;
  overflow: hidden;
  user-select: none;

  .carousel-indicator {
    position: absolute;
    z-index: 9999998;
    bottom: 0;
    left: 50%;
    transform: translateX(-50%);
    box-sizing: border-box;
  }
}